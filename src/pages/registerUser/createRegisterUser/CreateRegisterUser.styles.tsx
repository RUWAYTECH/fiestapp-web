import { makeStyles } from "tss-react/mui";

const useStyles = makeStyles()((theme) => ({
    image: {
        minWidth: '30%',
        maxWidth: '70%',
        height: 'auto',
      },
}));

export default useStyles;
